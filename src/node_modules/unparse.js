"use strict";

var _ = require("lodash");

module.exports = unparse;

function unparse(pieces) {
  var piecesStr = _(pieces)
    .map(function(piece) {
      return [piece.left, piece.top, piece.right, piece.bottom].join(" ");
    })
    .join("\n");
  return pieces.length + "\n" + piecesStr;
}
