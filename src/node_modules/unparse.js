"use strict";

var _ = require("lodash");

module.exports = unparse;

function unparse(slices) {
  var stringOfSlices = _(slices)
    .map(function(slice) {
      return [slice.left, slice.top, slice.right, slice.bottom].join(" ");
    })
    .join("\n");
  return slices.length + "\n" + stringOfSlices;
}
