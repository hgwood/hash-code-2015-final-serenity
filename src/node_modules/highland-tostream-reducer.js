"use strict";

var highland = require("highland");

module.exports = function toStream(reducer) {
  var accumulator;
  return function toStreamReducer(memo, x) {
    if (!accumulator) accumulator = memo;
    accumulator = reducer(accumulator, x);
    return highland([accumulator]);
  };
};
